version: "3"
services:
  pgrouting:
    image: krashish8/pgrouting:11-2.5-main
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgres
volumes:
  db-data:
