version: "3"
services:
  pgrouting:
    image: krashish8/pgrouting:13-3.2-3.0
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=postgres
volumes:
  db-data:
